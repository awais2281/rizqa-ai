{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python main.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "$comment": "Serverless deployment: Configure in Railway dashboard Settings to enable Serverless mode (not Scale to Zero). This provides true serverless behavior with automatic scaling."
}


