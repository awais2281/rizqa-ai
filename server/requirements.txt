# FastAPI web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Hugging Face Transformers for Whisper
# Note: Version 4.40.0 is compatible with PyTorch 2.1.0 on Windows
transformers==4.40.0

# PyTorch (CPU-only for Windows compatibility)
# Note: Version 2.1.0 works reliably on Windows without DLL issues
torch==2.1.0
torchaudio==2.1.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.11.0  # For audio preprocessing (wavfile)

# NumPy (pin to <2.0 for compatibility)
numpy<2.0

# Utilities
requests>=2.31.0
