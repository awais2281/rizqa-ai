// Custom entry point to prevent update checks
import { registerRootComponent } from 'expo';
import App from './App';

// Register the main application component with Expo
registerRootComponent(App);

